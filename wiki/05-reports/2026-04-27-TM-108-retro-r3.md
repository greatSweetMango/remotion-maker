---
title: "2026-04-27 — TM-108 r3 회고: TM-111+TM-112 머지 후에도 multi-step 렌더 0/5"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, tm-108, ai/generate, qa]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude Opus 4.7 1M)
qa_iteration: 3
---

# TM-108 r3 — 회고

## 무엇을 했나

- TM-111(sanitize+fallback) + TM-112(SceneNParams evaluator) 모두 머지된 main 위에서 동일 5 케이스 재측정.
- baseline(single-shot) → 5/5 200 + judge 5/5 + avg 71.8/100 (r1 60.0 → r2 70.6 → r3 71.8 의 안정 상향).
- full(AI_MULTI_STEP=1) → 4/5 generate-200 (1건 server 500 회귀), 캡처된 4 케이스 모두 클라이언트 ErrorBoundary 노출.
- r1→r2→r3 추이 표 박제: [[2026-04-27-TM-108-quality-benchmark-r3|TM-108 r3 quality benchmark]].

## 잘 된 것

1. baseline 통제군이 회차 간에 흔들리지 않음 — 측정 신뢰성 확보.
2. TM-112 의 효과 부분 검증: r2 시그니처(`Scene1Params is not defined`) 가 r3 dev 로그에서 0 건으로 사라짐.
3. r1→r2→r3 시계열 데이터 누적 — multi-step 의 production 승격 적합성 판단 근거 마련.

## 개선/배운 점

1. **bench 결과 경로 충돌**: 다른 worktree 가 동시에 같은 `__tests__/benchmarks/results/tm-108/full/` 를 건드려 manifest 가 사라졌다. worktree-scoped 결과 경로 또는 PID suffix 격리가 필요.
2. **TM-111 fallback 의 실효성 의심**: case 1 server 500(SyntaxError) 에서 fallback 이 발동해도 못 살리거나 발동조차 안 했음 — fallback 트리거 조건을 parser/composer 에러까지 넓혀야 한다.
3. **multi-step 의 비결정성**: r2 에서 200 OK 였던 짧은 prompt 가 r3 에서 500 으로 회귀 — gpt-4o 출력 변동성에 대한 가드(코드 합성 후 syntax check) 추가 검토.
4. **evaluator 의 `<Unknown>` 컴포넌트 경로**: ErrorBoundary 가 `<Unknown>` 으로 표기 → default export 식별 실패. r2 의 SceneNParams 와 별개의 두 번째 실패 경로.
5. **본 회차 측정 시간 압박**: timeout 20분 + OpenAI 약 $1.5 예산 안에서 baseline 완전 + full 부분(judge skip) 으로 종료. 원래 의도한 "최종 비교 + 머지 권고" 까지 이르지 못함.

## 결론 / 다음 결정

- multi-step default-off 정책 유지 (ADR 후보). 본 r3 데이터는 production 승격을 지지하지 않음.
- 새 fix task `AI-BUG-multi-step-render-blank` spawn 권고 (parser 500 회귀 + `<Unknown>` EB 100%). triggers_requalify=[TM-108]. 머지 시 r4 자동 재실행.
- bench infra 소형 PR (results 경로 격리) — 다음 회차 측정 신뢰성 회복.

## 산출물

- 본 PR (코드 변경 없음, wiki 보고서 + retro 만):
  - `wiki/05-reports/2026-04-27-TM-108-quality-benchmark-r3.md`
  - `wiki/05-reports/2026-04-27-TM-108-retro-r3.md`
  - `__tests__/benchmarks/results/tm-108/baseline/{manifest,scores}.json` + screenshots/ (10 PNG)
- (full 결과 디렉토리는 동시 실행 충돌로 유실 — stdout 캡처에서 stat 복원만 가능.)

## 비용 / 시간

- baseline gen + judge: 약 $0.7
- full gen 4 건(judge 미수행): 약 $0.5
- 총 OpenAI ≈ $1.2 (예산 $1.5 미만)
- 총 wall time ≈ 18분 (timeout 20분 안).
