---
title: "2026-04-27 — TM-78 r2 회고"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, tm-78, qa]
status: active
report_type: session
period: "2026-04-27"
author: "TeamLead Agent"
---

# TM-78 r2 회고 — TM-91 머지 후 라이브 검증

## TL;DR

검증 자체는 잘 됐다 (18/18). 하지만 "라이브" 의 정의가 한 단계 좁아졌다 — Playwright `/studio` 가 사전 버그로 막혀서 jsdom + renderHook 으로 한 단계 추상화된 라이브 surface 까지만 도달. 진짜 브라우저 검증은 r3 로 미뤄짐.

## 잘된 점

- TM-91 의 리듀서 단위 테스트 (8개) 가 4 항목 중 3개 (1, 2, 3) 를 이미 직접 커버. 추가 작성한 라이브 테스트가 hook + UI 버튼 + 키보드 단축키 + 분기/메모리까지 검증.
- 메모리 누수 항목을 sentinel reference 추적 + future ref 교체 검증으로 대체 (heap snapshot 없이도 의미 있는 증거).
- 사전 빌드 버그를 발견 즉시 별건 fix task spawn + `triggers_requalify` 박제 → r3 자동 트리거 보장.

## 잘 안된 점 / 회고

- **Playwright 가 막혔다**: `/studio` 페이지가 prisma-on-client 빌드 에러로 안 뜸. main 의 사전 결함이라 본 task 영역 밖이었지만, "라이브 검증" 이라는 mission 일부를 다 채우진 못함.
- **task ID 충돌 우려**: 본 worktree 의 taskmaster 가 81 을 부여했는데, 다른 worktree (TM-81 multi-lang) 와 ID 가 겹침. Orchestrator main 머지 시점에 reconcile 필요 — 본 task 는 metadata 를 본문에 박아 두는 식으로 회피.
- **OpenAI 0 budget**: `task-master update-task --append` 가 PERPLEXITY 호출을 시도해 실패. JSON 직접 편집으로 우회 — 향후 budget=0 워크플로우에서는 metadata 박제 시 항상 직접 JSON 편집 권장.

## 발견한 이슈

1. (high) **AI-BUG-build-usage-prisma-client-bundle** — `src/lib/usage.ts` 가 top-level 에서 prisma 를 임포트하고, 클라이언트 컴포넌트 `PromptPanel.tsx` 가 `TIER_LIMITS` 만 쓰면서도 통째로 끌어와 `node:fs` 가 브라우저 번들에 들어감. 모든 worktree 의 dev 세션에서 `/studio` 차단. 권장 수정: `TIER_LIMITS` 만 별도 client-safe 파일로 분리, `usage.ts` 는 `'server-only'` 표기.

## 다음 회차 (r3) 전제

- TM-81 (build fix) 머지 → Orchestrator 가 `triggers_requalify=[TM-78]` 감지 → TM-78 자동 r3 trigger
- r3 에서 추가할 것: Playwright `/studio` 라이브 키보드 패스 (Cmd+Z spam 50회, Cmd+Shift+Z 분기 차단, 버튼 disabled 시각 검증, 실제 Chrome heap snapshot 비교)

## 메트릭

- spawned teammate: 0 (단순 검증, build-team 미사용)
- 새 의존성: 0
- 코드 변경 라인: 테스트 1개 추가 (`+264`), 코드 src/* 변경 0
- 테스트: 10 추가, 0 회귀 (기존 8 도 통과)
- LLM 비용: $0 (테스트 작성 + jest 만 사용)
