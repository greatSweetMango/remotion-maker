---
title: "2026-04-27 — TM-108 회고: 품질 벤치마크 / multi-step 가용성 회귀 발견"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro, bench, tm-108]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude Opus 4.7)
---

# TM-108 회고 — r1

**Verdict**: APPROVE (벤치 산출물 자체) / **REQUEST_CHANGES** (multi-step default-on 결정에 대해 → hold).

## 잘 된 것

- 5 케이스 + 2 모드 + judge 까지 단일 worktree 안에서 ~15 분 안에 종료 (예산 20 분 안).
- baseline 경로가 5/5 success / avg 60 점으로 **참조 베이스라인 확보** — 차후 회귀 감시 기준선.
- TM-66 judge 인프라 (gpt-4o multimodal, temp=0, seed=42) 를 거의 수정 없이 재사용 → 일관된 점수 축.
- **전혀 예상하지 못한 가용성 이슈 발견** — TM-102 multi-step + gpt-4o 조합이 sandbox 정책과 충돌해 5건 중 4건 throw.
  retro 가 TM-102 Sonnet 검증만으로는 안전하지 않다는 메시지를 명확히 남김.

## 아쉬운 것

- 처음 설계는 "5 케이스 × 3 프레임 × 2 모드" 였으나 시간 압박으로 2 프레임으로 축소 — motion 점수 신뢰도 약간 하락.
- full 경로에서 4건이 generate 단계에서 죽어 **품질 비교가 사실상 1 데이터 포인트** 로 줄어듦.
  → 효과 측정의 본래 목적은 미달성, 대신 "guard rail 역설" 을 발견.
- long-video case (2) 가 baseline 에서도 50 점 — capture 프레임 (90, 180) 이 1800-frame 영상의 첫 6초 안쪽이라
  intro 외 콘텐츠를 못 봄. long video 전용 capture 프레임 정책 (e.g. 25%/50%/75%) 이 필요.

## 다음에 할 것

1. **블로커**: TM-102 composer 에 post-validation sanitizer 추가 (`require`/`globalThis`/`import`) 또는
   해당 scene 폐기 후 single-shot fallback. fix task spawn 권고:
   - `AI-BUG-pipeline-sandbox-fallback` (priority high, deps=102/108).
2. multi-step 모델 정책 명문화 — Anthropic Sonnet 에서만 default-on 검토 (TM-102 retro 의 5/5 검증은 Sonnet 결과).
3. long-video capture 프레임을 동적 (durationInFrames 기반 25/50/75%) 로 바꿔 case 2 점수 재측정.
4. `n=5 / mode` 는 통계적으로 약함 — 다음 라운드는 10 케이스로 확장 (예산 ~$3).

## 락 해제

- TM-108 작업 중 다른 task 가 `__tests__/benchmarks/results/tm-108/*` 동시 수정하지 않도록 보호. 본 PR 머지와 함께 락 해제.

## spawned_tasks (권고)

```json
{
  "spawned_tasks": [
    {"id": "TBD", "title": "AI-BUG-pipeline-sandbox-fallback",
     "desc": "TM-102 composer must sanitize/forbid require·globalThis·import emitted by gpt-4o; or fall back to single-shot. Reproduces 4/5 of TM-108 full mode.",
     "triggers_requalify": ["108"]}
  ]
}
```

(자동 생성은 보류 — Orchestrator 측에서 우선순위/owner 검토 후 결정)
