---
title: "TM-18 회고 — 휴지통 30일 보관"
created: 2026-04-27
tags: [report, retrospective, feature]
status: active
---

# TM-18 회고

## 결과
- branch `TM-18-trash-30d` @ `a50e3fd`, 24 files +946/-58
- 220/220 tests (+23), 회귀 0
- TeamLead solo (c3, 단순 CRUD)

## 구현
- Schema: `Asset.deletedAt: DateTime?` + `@@index([userId, deletedAt])`
- API: DELETE /api/asset/[id] (soft), /api/trash routes (list, restore, hard delete)
- Read-path 6곳 (`dashboard`, `/api/assets`, `studio`, `/share/[slug]`, `fork`, `edit`, `export`) 모두 `deletedAt: null` 필터
- `/trash` 페이지 + AssetCard hover Trash button + dashboard nav

## 결정
- **Lazy purge**: 30일 이상 항목은 daily cron 대신 다음 trash 조회 시 자동 정리 (`purgeExpiredTrash(userId, now?)`)
- 권한 격리: 모든 trash route에 `userId` scope 필수

## Follow-up
- PR 환경 권한 막혀 Orchestrator가 별도 생성
- 머지 후 `npx prisma db push`
- 30일 cron 자동화는 옵션 (현재 lazy로 충분)
