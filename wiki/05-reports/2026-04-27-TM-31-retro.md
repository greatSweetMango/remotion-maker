---
title: "TM-31 회고 — 사용자 업로드 파이프라인"
date: 2026-04-27
type: retro
tags: [retro, uploads]
---

# TM-31 회고

## 결과
- 1시간 단일 세션, 17 files +1207/-22, 140→166 tests (+26)
- TeamLead 단독 (build-team subagent 생략 — 명세 명확)

## 잘된 점
- **Lazy import**: @vercel/blob 미설치 상태로 lint/typecheck/test 모두 통과 → 사람 어프루벌과 자동 영역 분리
- **인터페이스 우선**: StorageProvider → local/blob 동일 shape → 후행 S3 무비용
- **기존 패턴 답습**: share/route.ts auth → ownership → mutation, usage.ts TIER_LIMITS, extract-params.ts type 분기
- **lint 효과**: react-hooks/set-state-in-effect 룰이 ResourcePanel/ParameterControl cascade 잡음 → async IIFE로 수정

## 개선
- 폰트 @font-face 자동 로드 누락 — generate prompt 업데이트로 후행
- ResourcePanel useEffect refresh 의존성 무거움 — onChanged useCallback 안정화 가이드 필요
- storageProvider 컬럼: SQLite enum 미지원이라 string 검증

## 다음
1. PR #24 머지 → `npx prisma db push`
2. Vercel Blob 토큰 발급 (사람 액션)
3. 폰트 자동 @font-face 통합 (별도 task)
