---
title: TM-78 Retrospective — Customize Undo/Redo QA
date: 2026-04-27
type: retro
task_id: TM-78
verdict: REQUEST_CHANGES
qa_iteration: 1
tags: ["#retro", "#qa"]
---

# TM-78 Retro

## What happened
- TM-78은 customize 패널의 undo/redo 깊이/limit/분기/메모리 누수 4개 항목을 검증하는 QA task.
- 코드 리뷰 결과 **undo/redo 기능 자체가 미구현**. `UPDATE_PARAM` reducer가 단순 덮어쓰기, Cmd+Z 핸들러 없음, UI 버튼 없음.
- Playwright 자동화는 본 worktree(3078) cookie가 같은 127.0.0.1 host의 다른 worktree dev server(3079/3086)로 spilling되는 환경 이슈로 차단. 코드 검증으로 결론은 충분.
- fix task `AI-BUG-customize-undo-redo` spawn (high, triggers_requalify=[TM-78]).

## What went well
- 코드 진실(`useStudio.ts`)이 명확하여 5분 안에 verdict 도출.
- spec 4개 항목이 모두 N/A임을 단일 evidence chain으로 정리.

## What hurt
- dev-login redirect chain이 worktree 격리를 깨뜨림 (NEXTAUTH_URL은 worktree마다 port 다른데 NextAuth callback redirect가 다른 worktree URL을 hold).
- Playwright 검증을 위해서는 worktree마다 hostname 분리(localhost vs 127.0.0.1) + storageState 격리 필요. 미래 QA task에서 부담.

## Action items
- TM-78 fix task: history layer 구현 (past/future stack, debounce 250ms, depth limit 100, Cmd+Z handler).
- 후속 infra: worktree QA를 위한 hostname 분리 정책 명문화 (TM-65 후속 또는 새 ADR).

## Stats
- 1회차 QA. 코드 리뷰 + UI 시도 약 10분 소진.
- diff: docs only (qa.md, retro.md, context).
