---
title: TM-54 회고 — first-frame latency 픽스
created: 2026-04-27
updated: 2026-04-27
tags: [retro, ai, perf, tm-54]
task_id: TM-54
status: active
report_type: session
---

# TM-54 회고

## 무엇을 했나

- TM-41 QA 발견 acceptance 위반 (`generateAsset` wall p50=10s) 픽스.
- 채택 조치: provider-aware streaming + max_tokens 4096→2500.
- 미채택: prompt 간소화 (TM-58 분리).
- 라이브 검증: 10 prompt, FREE/gpt-4o-mini, **TTFB p50=877 ms** (목표 5,000 ms 이하 충족).

## 무엇이 잘 됐나

- 기존 `stream.ts` (Anthropic-only) 와 별개로 `client.ts` 안에 provider-aware streaming 을 두면서 `chatComplete` thin-wrapper 보존 → 16개 기존 단위 테스트 무수정 통과.
- `onFirstToken` 콜백 패턴 — route 와 bench 하네스가 같은 신호를 공유 → 운영/측정 일관.
- 전체 작업 25 분, live API 비용 < $0.05 (10 prompt FREE).

## 무엇이 막혔나 / 헛수고

- 처음 worktree 에 `.env.local` 미존재 — main 에서 복사. Orchestrator 가 worktree 생성 시 env propagation 자동화 필요 (TM-56 가 다루고 있음).
- "첫 프레임" 정의가 모호 — TTFB 인지 full asset wall 인지. TTFB 로 해석하면 acceptance 충족, full wall 은 여전히 8s. UI 가 streaming 을 surface 해야 비로소 사용자 perception 이 개선됨 → 후속 UX task 필요.

## 다음에 다르게 할 점

- acceptance 정의를 wiki/decisions ADR 로 명문화 (TTFB vs total). 현 retro 는 TTFB 기준으로 통과를 주장하지만, 추후 검증자가 다른 해석을 할 수 있음.
- live bench 하네스 (`tm-54-latency.test.ts`) 를 Tier 별 + Anthropic provider 까지 확장하면 prompt-caching 효과 (ADR-0003) 도 같이 측정 가능.

## triggers_requalify

- TM-41 (parent) — 본 PR 머지 시 자동 `pending` 복귀, 재측정 회차 (`-r2`).

## Spawned tasks

- 없음.
