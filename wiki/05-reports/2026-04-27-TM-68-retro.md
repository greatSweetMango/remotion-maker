---
title: TM-68 retro — entity-count clarify gate
date: 2026-04-27
type: session
tags: [retro, ai, tm-68]
task_id: TM-68
status: active
---

# TM-68 — Retro

## 잘 된 점

- TM-52 의 추상화가 정확히 옳았기 때문에 확장 비용이 작았다 — `ConcretenessReport` 에 두 필드 추가, 새 reinforcement 한 개, generate.ts 분기 한 개.
- 시간 단위(초/s/fps) 와 entity counter 를 명시적으로 분리한 게 ig-03 케이스 (`30s vs 5s`) 가 false-trigger 되는 걸 자연스럽게 막아줬다. 테스트로 박제.
- 32개 테스트 — KO/EN/grid/duration-exclusion/e2e 까지 매트릭스로 깔아 회귀 안전망.

## 아쉬운 점 / 한계

- TM-68 은 dv-05/ig-02 류 (entity count ≥ 2) 만 해결한다. ig-03 같이 **시간 비교** 로만 specificity 를 표현한 prompt 는 TM-52 까지로 끝나며, hardened retry 의 인용할 숫자가 없어 TM-68 path 가 발화하지 않는다. 보고서에 명시했고 후속 task 필요.
- LLM 이 hardened retry 에서까지 clarify 를 또 반환할 가능성은 없지 않다. 그 경우 그대로 surface (추가 retry 없음) — 무한 루프 방지 우선.
- ig-02 는 사실 clarify 가 아니라 transpile fail 이라 TM-68 과 무관. TM-66 이 별도로 잡아야 함.

## 다음 r3 재검증 시 주목할 지점

- `[generateAsset] entity-count override (TM-68)` warn 로그 발화 횟수 — dv-05 prompt 에서 1회 이상 떠야 정상.
- 만약 hardened retry 까지 clarify 라면 LLM-side prompt 자체를 더 손봐야 함.
- dv-05 pass / fail — pass 면 TM-68 효과 입증, fail 이면 dv-05 의 본질이 entity-count 가 아닌 다른 신호 부족 (예: 모델 자체 한계) 임을 시사.

## 락 해제

- `.taskmaster/tasks/tasks.json` TM-68 → status `done` 으로 마킹 (Orchestrator가 PR 머지 후 처리).
- TM-41 의 r3 재검증은 TM-66 / TM-67 / TM-68 모두 머지 후 트리거.
