---
title: "2026-04-27 — TM-66 회고: judge 마이그레이션 / 락 해제"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude)
---

# TM-66 회고

## 잘 된 것

- escalate 원인 진단이 빨랐음 — 메인 vault `.env.local` 확인만으로 `ANTHROPIC_API_KEY=` (빈 값) 식별. worktree 에 심볼릭 링크 1줄로 환경 통일.
- judge 의 의존성 분리 결정 — 코드 생성(Anthropic) 과 visual judge(OpenAI) 를 다른 vendor로 운영하는 게 비용/리스크 양면에서 합리적이라는 판단.
- mock-driven 단위 테스트로 SDK 교체의 회귀 위험을 빠르게 격리. 라이브 smoke 는 단 1회 호출(~$0.01)로 제한.

## 아쉬운 것

- placeholder 1×1 PNG 로는 모델이 실제 시각 분석을 못함 → smoke는 파이프라인 검증만 가능. 진짜 점수 신뢰도는 capture 단계 합류 후 30-prompt full-run 으로 검증해야 함.
- 두 vendor 운영으로 키/쿼터/장애 surface 가 늘어남. judge 다운 시 fallback 모델 정책 미정.

## 다음에 할 것

- TM-46 escalate 해제 + 30-prompt full-run.
- gpt-4o vs Opus 4.7 점수 drift 측정 (동일 screenshots set 가능 시).
- judge fallback chain 정의: gpt-4o → claude-sonnet-4 (key 가 있을 때) → 점수 N/A.

## 락 해제

- TM-66 작업 중 다른 잡이 `__tests__/benchmarks/tm-46-*.ts` 동시 수정하지 않도록 lock 사용했다면 본 PR 머지와 함께 해제.
