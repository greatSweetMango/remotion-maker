---
title: "2026-04-27 — TM-60 회고"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retrospective]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (TM-60)
task_id: TM-60
---

# TM-60 회고

## 잘 된 점

- TM-43 audit이 단서를 잘 제공해서 문제 위치(TypeExplosion.tsx)와 phase 타이밍이 즉시 추정됨.
- 코드 분석 → spring 시뮬레이션 → 코드 수정 → live screenshot 순서로 빠르게 진행 (≈30분).
- Spring 파라미터 변경 효과를 코드 변경 없이 node 시뮬레이터로 미리 검증 → 한 번에 의도한 타이밍 분포 확인.

## 어려웠던 점

- **Cookie origin shared 이슈** 재발: 다른 worktree(TM-41~ 등)들이 `127.0.0.1:30xx`을 점유 중이어서 navigate 시 next-auth session-token이 cross-port로 공유되며 다른 worktree로 redirect됨. dev-login 후 직접 클릭으로 우회. TM-43 보고서에 이미 기록된 known issue. `__Host-` cookie prefix나 worktree별 다른 호스트(`127.0.0.2` 등) 분리가 영구 해결책 — 별도 infra task로.
- Dev 서버가 한 번 죽었음 (`/tmp/tm60-dev.log`). 재기동 후 정상. 원인 미상 (slow filesystem warning만 로그에 있음). 자주 발생하면 별도 조사 필요.

## 다음에 적용할 점

- 시각 audit 캡처 timing은 wall-clock(2초/3초/4초) 대신 **frame seek API**로 명시 frame을 캡처해야 함 — 자동재생/loop이 brower에서 멈출 수 있어 같은 frame이 mid/end에 잡힘. TM-46 작업 시 dev player에 `?seek=N` 같은 디버그 query 추가하면 audit 안정성↑.

## 메트릭

- 코드 수정 라인: ~25 lines (1 파일).
- 시각 검증: dev server live, 3 screenshots, fix 확인.
- LLM 호출: 0
- OpenAI 비용: $0
