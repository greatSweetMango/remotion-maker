---
title: TM-37 Retrospective — ScheduleWakeup idle 자동화
created: 2026-04-27
tags: [report, retrospective, infra]
status: active
---

# TM-37 회고

## 결과
- PR #20, commit `80454ce`, 1 file +31/-6 (`.claude/commands/orchestrate.md`)
- TeamLead solo (docs-only, complexity 3)

## 핵심 명확화 (Step 7-3)
1. ScheduleWakeup 호출을 의사코드 → 실제 인자(`delaySeconds`/`prompt`/`reason`) 명시 객체
2. **delaySeconds 매트릭스**: continuous=1800, once/max_n=900, **300 금지** (5분 cache TTL — worst-of-both 회피)
3. **prompt 합성**: `args.length == 0 → "/orchestrate"`, 그 외 `"/orchestrate " + args.join(" ")`. 원본 `$ARGUMENTS` 보존 누락 시 mode 리셋 함정 명시
4. `reason` 필드 형식 (telemetry) + idle transcript 1-line 로그 컨벤션
5. **종료 vs idle 분기 표**: STOP/budget/loop-count/forced/완료된 once·max_n은 exit, 그 외 ScheduleWakeup default
