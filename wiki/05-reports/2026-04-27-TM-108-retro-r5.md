---
title: "2026-04-27 — TM-108 r5 회고: TM-116 머지 후 부분 측정 + 회귀 관찰"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, tm-108, ai/generate, qa]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude Opus 4.7 1M)
qa_iteration: 5
---

# TM-108 r5 — 회고

## 무엇을 했나

- TM-116 (evaluator 28 globals 확장 destructure + per-scene `__SceneBoundary` + displayName, PR #117) 머지된 main 위에서 동일 5 케이스 재측정 시도.
- baseline 5/5 완주 → avg **69.8** (r4 72.4 대비 -2.6, 첫 하락).
- full 측정 중 case 4 chart 가 30s+ generation + Scene2 EB 발생, bench timeout 한도 근접하여 인터럽트. case 5 미실행.
- 부분 capture(case 2, 3) 만 별도 judge 스크립트(`tm-108-r5-judge.ts`)로 채점 → avg **65.0 (n=2)**.

## 잘 된 것

1. **per-scene `__SceneBoundary` 효과 관찰**: case 4 chart 에서 `<Scene2>` 만 EB 표시 — 어느 scene 이 깨졌는지 가시화. r4 의 통합 `<Unknown>` EB 대비 디버깅성 개선.
2. **case 3 url-ingest 점수 회복**: r4 13 → r5 80. `__SceneBoundary` 가 깨진 scene 격리 후 정상 scene 은 렌더되는 효과 시사.
3. **bench 인터럽트 후 부분 결과 보존**: manifest 가 case 별로 incremental 저장되어 있어 case 1-3 데이터 + 별도 judge 스크립트로 부분 보고서 작성 가능.

## 개선/배운 점

1. **클린 렌더 0/5\* (부분측정) — TM-116 단독으로 부족**: r4 의 2/5 진보가 r5 에서 (측정 가능한 한) 사라짐. EB 디스플레이 개선이 코드 품질 자체를 고치진 못함.
2. **간헐 회귀 (`SyntaxError 1:46`)**: case 1 이 r3 에서 발생 → r4 에서 해소(TM-114) → r5 에 재발. TM-114 의 sanitize 정규식이 LLM 출력의 모든 variant 를 잡지 못함 → 추가 fix 필요.
3. **bench 시간 예산 초과**: chart case 가 30s gen + 1-2 frame capture (각 2.5s) + judge 까지 합치면 1 케이스만 ≈1min 30s. 5 케이스 직렬 + 모드 2개 + dev 재시작 → 20min timeout 안에 완주 마진 없음. **bench 의 case 4/5 부분 capture-only 모드** + 별도 judge 스크립트 분리가 향후 표준 필요.
4. **baseline 안정성**: 통제군 case 4 점수가 r4 65 → r5 42 로 크게 흔들림. LLM 비결정성 노이즈가 ±10~20 점 — 1회 측정만으로 회차 비교는 위험. seed/run 평균 2-3회 도입 검토.

## 결론 / 다음 결정

- **multi-step default-off 유지** — r5 도 클린 렌더 0/5\* 확인. r6 재측정으로 case 4/5 보강 후 ADR 정량 근거 완성.
- **TM-108 r6 자동 트리거 후보**:
  - `AI-BUG-multi-step-evaluator-unknown-eb` (잔존 `<Unknown>` EB) — triggers_requalify=[TM-108]
  - `AI-BUG-sanitize-1-46-flaky` (간헐 1:46 회귀) — triggers_requalify=[TM-108]
- **bench infra 우선순위 상승**: case 병렬 + capture/judge 분리 + 재실행 시드 평균 → r6 측정 신뢰도 확보 전 필수.

## 산출물

- 본 PR (코드 변경 없음, wiki + 부분 측정 결과):
  - `wiki/05-reports/2026-04-27-TM-108-quality-benchmark-r5.md`
  - `wiki/05-reports/2026-04-27-TM-108-retro-r5.md`
  - `__tests__/benchmarks/results/tm-108/baseline/{manifest,scores}.json` + screenshots/ (10 PNG)
  - `__tests__/benchmarks/results/tm-108/full/{manifest,scores}.json` + screenshots/ (4 PNG, case 2/3 만)
  - `__tests__/benchmarks/tm-108-r5-judge.ts` (부분 capture 재judge 스크립트)

## 비용 / 시간

- OpenAI: gpt-4o judge 7회 (baseline 5 + full r5 부분 2) + multi-step generation 4회 시도 (3 성공 + 1 500). 추정 ~$1.2.
- 시간: setup → baseline (≈4min) → dev 재시작 → full 부분 capture (≈6min, case 4 에서 timeout 인터럽트) → 부분 judge (≈1min) → wiki 작성. 총 ≈18min.

## 다음 회차 트리거

- bench timeout 확장 + capture/judge 분리 PR 머지 → r6 전체 5/5 재측정.
- 또는 위 두 AI-BUG fix 중 하나 머지 → r6 자동 재실행 (qa_iteration=6).
