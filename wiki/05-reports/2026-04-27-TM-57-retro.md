---
title: 'TM-57 Retrospective — zero-width prompt validation'
created: 2026-04-27
updated: 2026-04-27
tags: [report, retrospective, validation, agent-company]
status: active
report_type: session
---

# TM-57 Retrospective

## 진행

- TeamLead 단독 실행 — 단일 함수 강화에 단위 테스트 추가, 외부 리서치 0.
- worktree 가 outdated main(=8be6bba) 기반이라 `src/lib/validation/prompt.ts` 가 부재 → `git rebase origin/main` 으로 TM-58 코드 위에 올린 뒤 시작.
- node_modules 부재 → main repo 의 `node_modules` 를 symlink 해 jest 실행. 18 tests pass.

## 잘된 것

- 정규식의 invisible 문자를 `\uXXXX` escape 로 정규화 → grep / 코드 리뷰 친화.
- 길이 cap 검사는 원본 `.length` 유지 → invisible 패딩으로 cap 우회하는 2차 공격 동시 방어.
- TM-45 case A4 를 직접 재현하는 test case 작성으로 회귀 신호가 명확.
- `stripZeroWidth` 를 export 해서 향후 호출자가 LLM 입력 정제에도 재사용 가능.

## 아쉬운 것

- worktree 가 outdated main 기반으로 만들어진 채 위임됐다 → orchestrator 가 worktree 생성 시점에 `git fetch origin && git rebase origin/main` 을 항상 수행하도록 표준화 필요.
- `scripts/setup-worktree.sh` 가 main 브랜치에 아직 없어서 (TM-56 머지 후에도 worktree 가 그 이전 base) 부트스트랩 스크립트 사용 불가 → symlink 우회. orchestrator가 항상 최신 base에서 worktree를 생성하면 자동 해결.
- BOM(U+FEFF) 은 엄밀히 "zero-width no-break space" 인데 Unicode whitespace 카테고리에 포함되지 않아 `.trim()` 이 못 잡는다는 점은 일반적으로 가려져 있다 — 추후 다른 validator(파일명, 검색어 등)도 동일 패턴일 가능성, audit 후속 task 로 등록 검토.

## 다음에 가져갈 결정

- 사용자 입력을 받는 모든 validator에 `stripZeroWidth` 또는 동일 정책을 적용하는 것이 디폴트가 되어야 한다.
- worktree 위임 시 orchestrator는 base sync 를 보장한다 (체크리스트 추가 검토).

## 메트릭

- 변경 파일: 2 (`src/lib/validation/prompt.ts`, `__tests__/lib/validation-prompt.test.ts`)
- 추가 테스트: 12 (총 6 → 18)
- OpenAI 호출: 0
- 소요: ~10 min
