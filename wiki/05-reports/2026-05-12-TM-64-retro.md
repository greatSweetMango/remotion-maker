---
date: 2026-05-12
type: retro
task: TM-64
title: AI-FIX-csp-media-src-data-allow — DUPLICATE (이미 TM-50 에서 처리)
tags: [retro, csp, duplicate, ref/TM-50, ref/TM-43]
verdict: NO-OP / CLOSE-AS-DUPLICATE
---

## TL;DR

**NO-OP.** TM-64 가 요구하는 변경(`next.config.ts` `SANDBOX_CSP` `media-src` 에 `data:` 추가) 은 이미 **TM-50** 에서 처리되어 main 에 머지되어 있음 (commit `a929018`, PR #39, 2026-04-27). TM-43 r2 (PR #46) 가 35/35 템플릿에서 CSP violation 0 을 재확인. 코드 변경 없이 task 를 종결 권고.

## 조사

- 워크트리 `fix/tm-64-csp-media-src-data` 는 `origin/main` (5a8efb3) 과 동일. `git diff origin/main -- next.config.ts` → **빈 diff**.
- 현재 `next.config.ts:24` :
  ```
  "media-src 'self' data: blob: https:"
  ```
  주석으로 TM-50 컨텍스트 명시되어 있음 (line 21-23).
- 회귀 가드: `__tests__/config/csp-headers.test.ts` — `npm test` 통과 (3/3). 향후 누군가 `data:` 를 제거하면 CI 에서 차단.

## 왜 TM-64 가 생성되었나

- TM-44 (customize roundtrip QA) 와 TM-43 (visual audit r1) 에서 동일 증상을 발견하고 각자 follow-up task 를 spawn → **TM-50** (TM-44 측) 와 **TM-64** (TM-43 측) 가 중복 생성.
- TM-50 가 먼저 처리되어 머지됨. TM-64 는 dependency=[43] 으로 등록되었으나 실제 작업물은 0.
- TM-43 retro r2 (2026-04-27) 가 이미 `media-src 'self' data: blob: https:` 정상 적용 및 신규 이슈 0 을 보고.

## 권고

- TM-64 → `done` 상태로 closing, notes 에 "duplicate of TM-50, validated by TM-43 r2" 기록.
- TM-43 requalify 트리거 불필요 — 이미 r2 에서 처리됨.
- 향후 task-master parse 시 fix-task duplicate detection (같은 area + 같은 키워드) heuristic 검토 가치 있음 (개선 task 가 필요하면 별도 spawn).

## 변경 사항

- 코드: 0
- 테스트: 0
- 문서: 본 retro 만 추가.

## 관련

- TM-50 PR #39 — 실제 fix.
- TM-43 PR #46 — 35/35 재검증.
- TM-44 retro 2026-04-27 — bug 발견 origin.
