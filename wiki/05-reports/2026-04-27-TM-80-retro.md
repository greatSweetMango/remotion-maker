---
title: "2026-04-27 — TM-80 retro"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro, TM-80]
status: active
report_type: session
period: "2026-04-27"
author: "TeamLead/TM-80"
---

# TM-80 — Retrospective

## TL;DR

a11y audit baseline 확보 완료. critical 21건 발견 → fix task spawn 권고. OpenAI cost = 0 (자동 측정만). Phase 진행 시간 ~4분(setup + npm install 포함).

## 잘된 점

- `scripts/qa/` 패턴 따라 standalone Node mjs 스크립트로 작성 → CI/manual 양쪽 재사용 가능.
- `DEV_AUTO_LOGIN=true` 활용해 auth 페이지 진입 자동화. cookie 파일 따로 안 만들어도 됨.
- axe 룰을 WCAG2A/2AA tag 로 한정 → noise 최소화(best-practice 룰 제외).
- 3 핵심 페이지 + 5 템플릿 = 8 페이지 한번에 audit, 단일 결과 JSON 으로 회귀 비교 가능.

## 아쉬웠던 점 / 향후 개선

- modal/menu 열림 상태 audit 미포함 (Export, Share, Resize). 차기 fix task 에서 추가 시나리오 필요.
- `withoutVisibleLabel` 검출 휴리스틱은 icon-only `<button>` 을 과탐 가능 — `aria-labelledby` 까지 검사하도록 보강 권장.
- Tab walk 30회 캡 → interactive=68(customize) 페이지에선 일부 마지막 단 미도달. 캡 동적 조정 필요.
- shadcn 컴포넌트 다수가 wrapping `<Label>` 없이 사용됨 → 컴포넌트 단위 fix 가 아니라 design-system 단위 가이드라인 필요(ADR 후보).

## 후속 작업 후보

- **TM-80-fix**: ParameterControl/Slider/Switch label 보강 + Tailwind contrast token 조정 (triggers_requalify [80]).
- **CI hook**: GitHub Actions 에 `node scripts/qa/tm-80-a11y-audit.mjs` 추가, totals.critical=0 gate.
- **a11y design-system ADR**: shadcn 사용 시 라벨링 룰 표준화.

## 비용

- OpenAI: $0.00 (LLM 호출 없음 — pure browser audit)
- Time: 약 4 분 (worktree setup + npm install playwright + browser download + audit run)
