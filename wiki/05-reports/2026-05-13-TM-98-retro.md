---
title: TM-98 — Specialized agent ai-prompt-tuner
date: 2026-05-13
type: retro
task_id: TM-98
related: [TM-83, TM-85, TM-92, TM-93, TM-95, TM-106]
tags: [#docs, #infra, agent-company]
---

# TM-98 — Specialized agent: `ai-prompt-tuner`

## 요약

AI prompt 도메인 (system prompt / clarify-gate / generate / classify) 변경 task 를 전담할 **특화 TeamLead agent** 를 정의했다. TM-83 / TM-85 / TM-92 / TM-93 / TM-95 / TM-106 으로 이어진 prompt 튜닝 사이클의 표준 owner 를 명문화 — 매번 generic TeamLead 가 처음부터 cache_control / clarify rule / 회귀 벤치 컨벤션을 재학습할 필요 없도록 SOP 와 검증 매트릭스를 박제.

본 PR 은 **정의 / 라우팅 / 문서만** 포함하고 실제 dispatch 는 후속 task 로 분리 (Phase D smoke 박제).

## 산출물

- `.claude/agents/ai-prompt-tuner.md` (신규) — frontmatter (name / description / tools / model=opus) + 본문 (touch surface / ADR 매핑 / 표준 검증 매트릭스 / SOP 차이점 / 트리거 / 금지).
- `.claude/agents/pm.md` (수정) — `area` 필드 라우팅 룰 추가. `teamlead_agent` schema 항목 도입.
- `prompts/team-lead.md` (수정) — Phase B-pre 단계 추가, `area="ai-prompt"` 인 경우 `Task({subagent_type: "ai-prompt-tuner", ...})` 위임 패턴 명시.

## 라우팅 정책

PM router (`.claude/agents/pm.md` §핵심 책임 2):

| 조건 | teamlead_agent |
|---|---|
| `context_files` 에 `src/lib/ai/{prompts,clarify-gate,generate,classify,clarify-questions}.ts` 중 하나 포함 | `ai-prompt-tuner` |
| title/desc 에 "system prompt / clarify / mode_match / pipeline quality / prompt regression / 프롬프트 튜닝" | `ai-prompt-tuner` |
| 의존 task ∈ {TM-83, TM-85, TM-92, TM-93, TM-95, TM-106} | `ai-prompt-tuner` |
| 그 외 | `null` (generic) |

## 표준 검증 매트릭스 (agent 가 매 PR 마다 강제)

1. `pnpm typecheck` + `pnpm lint`
2. `node scripts/qa/tm-83-clarify-regression.mjs` — clarify-gate 회귀
3. `node scripts/qa/tm-85-pipeline-quality.mjs` — mode_match ≥ 28/30, data-viz 5/5 generate
4. dev server live sanity (living-entity 1 / data-viz 1 / static 1)

회귀 발견 시 verdict 강등 + `triggers_requalify` 박제.

## 검증 (본 PR)

- 정의 / 문서만 변경 — 코드 touch 없음. typecheck/lint 영향 없음.
- frontmatter 파싱: `name`, `description`, `tools`, `model` 모두 기존 agent (`pm.md` / `planner.md` / `marketer.md`) 와 동일 schema.
- `tools` 화이트리스트는 prompt 튜닝 task 에 필요한 최소 셋 (Bash / Edit / Read / Grep / Glob / Task / Skill + TM/context7 MCP).

## 후속 (spawned)

- **smoke dispatch**: 다음 AI prompt 튜닝 task 발생 시 PM 이 `teamlead_agent: "ai-prompt-tuner"` 부여 → `Task({subagent_type:"ai-prompt-tuner", ...})` 실제 호출 + 본 retro 와 비교.
- agent description / 트리거 키워드는 1~2회 실사용 후 회고 통해 튜닝 예정.

## 환경

- worktree: `worktrees/TM-98-prompt-tuner`
- branch: `feat/tm-98-prompt-tuner-agent`
- base: `08e1749` (post TM-106)
