---
title: TM-58 retrospective — prompt length cap
created: 2026-04-27
updated: 2026-04-27
tags: [retrospective, ai, security]
status: active
report_type: session
task_id: TM-58
---

# TM-58 회고

상세 변경 내역 + 검증 결과는 [[05-reports/2026-04-27-TM-58-fix]] 참조. 본 노트는 진행 회고.

## 진행

- 작업 시간 ~15분 (단일 단계, build-team skip — complexity=low)
- 코드 +94 / -2 (대략), 테스트 13 added, 0 regression
- typecheck: TM-58 파일 0 errors (pre-existing TM-48 fuzz 테스트 에러는 본 task 무관)
- ESLint: clean

## Keep

- **공유 검증 유틸 분리**: `src/lib/validation/prompt.ts` 로 추출 → 두 라우트 모두 동일 동작 보장 + 단위 테스트가 라우트 모킹 없이 빠름
- **DB 호출 이전 차단**: `/api/generate` 에서 prompt validation 을 user lookup 보다 먼저 배치 — 쿼터 burn 방지를 명시 테스트로 검증
- **테스트가 부재 검증**: "generateAsset not called" / "DB not touched" 를 명시 → 회귀 시 정확한 시그널

## Improve

- 사용자향 메시지를 한국어로 갔는데 i18n 인프라 없음 → 다른 에러 메시지들과 톤이 다를 수 있음. 향후 i18n task 시 일괄 정리 필요.
- 클라이언트 가드 (글자수 카운터 + disable) 는 별도 follow-up task 후보 — 본 task scope 밖.

## Try

- TM-57 (zero-width strip) 에서 같은 `validation/prompt.ts` 파일에 normalizer 추가 패턴 재사용
- 추후 PRO 등급별 cap 차등 도입 시 → ADR 작성

## Triggers requalify

- TM-45 (`triggers_requalify=[45]`) — 머지 시 Orchestrator 가 자동 회귀
