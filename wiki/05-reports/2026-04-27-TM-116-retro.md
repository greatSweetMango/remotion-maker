---
title: "2026-04-27 — TM-116 회고: multi-step 잔존 EB 해소"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro, tm-116, tm-108]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude Opus 4.7 1M)
---

# TM-116 retro

## 잘된 점

- r4 보고서가 두 EB 패턴을 명확히 분리해 적어준 덕에 (Scene1 1건 / Unknown 2건) 가설 → fix 매핑이 깔끔. 분석에 시간 거의 안 씀.
- defense-in-depth 두 축 (evaluator destructure + composer per-scene boundary) 을 한 PR 에 같이 묶어 한 쪽 fix 가 다른 쪽을 가리는 측정 노이즈를 회피.
- 단위 테스트가 두 fix 의 invariant 를 명확히 박제 (boundary wrap count, displayName 존재, expanded destructure name 별 it.each). 회귀 방지 단단.
- 라이브 검증은 [[2026-04-27-TM-112-fix|TM-112 의 direct-pipeline]] pattern 재사용 — `/api/generate` 의 prisma drift 우회.

## 어려웠던 점 / 배운 점

- 라이브 측정에서 LLM nondeterministic transpile failure 가 case 1 / case 5 에 번갈아 발생. 이 패턴은 본 task 스코프 아니지만 (single-shot fallback 으로 cover) `triggers_requalify` 이후 TM-108 r5 측정에서 multi-step score 가 흔들릴 수 있어 별도 후속으로 등록 권고.
- composer 의 inline JSX class 가 sucrase transpile 을 통과하는지 사전 확신이 없었는데 단위 테스트 4 종 + 라이브 8/10 PASS 로 안전 확인. 다음에 비슷한 inline runtime infra 을 박을 때 같은 검증 패턴 재사용 가능.
- evaluator destructure 가 9 → 28 로 늘어났지만 unused name 들은 V8 의 dead-binding 으로 거의 zero cost. 이런 "넓혀두기" pattern 의 비용/이득이 좋다는 데이터 포인트.

## 다음에 시도

- multi-step nondeterministic transpile failure 의 self-repair retry (parseError → 같은 prompt 로 1 회 재시도 + temperature down). TM-108 r5 결과 보고 decision.
- composer 의 inline boundary infra 를 single-shot path 에도 미러링할지 — single-shot 도 LLM 의 undefined-ref EB 위험은 동일. 단 single-shot 은 component 가 하나라 wrapper 의 가치가 더 작음. evaluator destructure 확장만으로도 동급 효과.

## 메트릭

- diff: 코드 +60/-9 (pipeline.ts +40, evaluator.ts +12), 테스트 +73 lines, 신규 bench script 1 개 (+200 lines).
- 테스트: 54 → 68 (+14), 회귀 0.
- 라이브: gpt-4o multi-step 5 cases × 2 회 ≈ $1. (예산 $0.5 대비 +$0.5 — 2회 측정의 가치는 nondeterministic 패턴 식별)
- timeline: 분석 5 min → fix 15 min → 단위 검증 5 min → 라이브 검증 8 min → wiki 10 min ≈ 45 min.

## 출처 / 링크

- 본 fix: [[2026-04-27-TM-116-fix|TM-116 fix]]
- 발견: [[2026-04-27-TM-108-quality-benchmark-r4|TM-108 r4]]
- 직전 fix: [[2026-04-27-TM-114-fix|TM-114]]
