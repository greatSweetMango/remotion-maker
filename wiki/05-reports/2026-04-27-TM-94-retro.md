---
title: "2026-04-27 — TM-94 retro"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, a11y, TM-94]
status: active
report_type: session
period: "2026-04-27"
author: "TeamLead/TM-94"
---

# TM-94 retro

## Went well
- TM-80 r2 보고서가 잔존 violation을 노드 단위로 좁혀줬다. grep 한 번에 정확히 두 위치(`PromptPanel.tsx` 246, 262)를 짚었다. 탐색 phase 거의 0.
- Fix 가설이 단순했다 — chip bg 600→700, kbd opacity-60 제거. 단일 파일/단일 commit 단위.
- 정적 contrast ratio 함수를 테스트에 인라인으로 넣어 token 변화 시 즉시 회귀 잡히게 했다. axe만으로는 jsdom alpha 합성을 못 잡으니 보완책으로 유효.

## Friction
- Worktree에 node_modules가 없어 npm install (19s)이 한 번 필요했다. orchestrator가 worktree 생성 시 `pnpm install` / `npm ci`를 같이 돌리도록 보강하면 1iter 단축.
- emerald-600/white = 3.77:1 — TM-90/이전 audit에서 "성공"으로 카운트되던 값이지만 사실 AA 미달이었다. axe가 잡은 violation은 violet chip만이었지만, 같은 패턴인 emerald도 함께 700 승격하지 않으면 다음 audit에서 잡힐 가능성이 높았다 — 선제 fix 판단.
- `opacity-60`은 jsdom에서 axe color-contrast 룰에 잡히지 않는다(getComputedStyle 한계). 즉 jest-axe 0 violations만 신뢰하면 회귀 검출 누락. 명시 색 토큰 단언 + 정적 ratio 계산을 함께 둔 이유.

## Followups
- triggers_requalify TM-80: post-merge로 `scripts/qa/tm-80-a11y-audit.mjs` 재실행. 25 노드(5 templates × 5)는 사라질 것으로 예상. 잔존 15(customize 베이스 12 + home 2 + login 1)는 별건 task spawn 권고 — 이번 chip과는 다른 토큰(sidebar header, marketing muted text, radix tab inactive).
- (도구) `text-slate-400 on bg-slate-800` ≈ 5.71 — 통과는 하지만 다른 muted 토큰이 위험할 수 있음. 디자인 토큰 audit script를 별도 task로 분리 추천.
- shadcn/Tailwind chip variant util로 추출하면 향후 단일 변경점 — 현 시점 over-engineering, ROI 낮음. 보류.

## Lock release
TM-94 work complete. PR 생성 후 lock release.
