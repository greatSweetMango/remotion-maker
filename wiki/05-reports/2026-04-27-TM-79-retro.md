---
date: 2026-04-27
type: retrospective
task: TM-79
title: TM-79 retro — Player controls QA (r1, partial)
tags: [retro, qa, player]
iteration: 1
---

# TM-79 retro — r1, partial

**Verdict**: REQUEST_CHANGES — landed 2 actionable findings (F1 missing playback-rate UI; F2 cross-worktree port bleed) but only 4/12 cases verified. Recommend r2 after addressing F2 env friction.

## What worked
- Fast diagnostic loop on Prisma `User` table missing — `prisma generate` + dev restart unblocked auth in <3 min.
- Code-side audit of `PlayerPanel.tsx` produced the F1 finding **without** needing to fully drive UI — `playbackRate` / `showPlaybackRateControl` props missing is a deterministic conclusion.
- Pause UI click → state flip verified cleanly via `aria-label` toggle.

## What didn't
- Cross-worktree dev servers (`:3074/79/81/84`) bled into each other during navigation. Page jumped from `:3079` to `:3084` mid-test, breaking key-press cases. Lost ~3 min before deciding to ship partial.
- Initial viewport was 375px wide (mobile) — Studio renders mobile shell with bottom tabs and no Player visible. Burned ~1 min before resizing to 1440×900.
- Did not stop sibling dev servers at start — cheaper to do upfront next time.

## Lessons
- **Pre-flight**: kill all `localhost:30??` dev servers except the target before any Playwright QA across worktrees.
- **Default viewport**: Playwright MCP defaults to mobile-ish; explicitly resize to desktop at the very first call when QA-ing desktop-only routes (Studio).
- **Code audit first**: For "is X exposed?" questions, grep the component props before driving the UI — saved a round trip on F1.

## Output
- wiki: `wiki/05-reports/2026-04-27-TM-79-qa.md` + `screenshots/TM-79/counter-loaded.png`
- code: no changes (QA-only task)
- spawned: 0 (recommended F1, F2 escalations deferred to next iter)

## Cost
- agents: 0 (TeamLead solo)
- tokens: ~modest single-session (no team spawn, no OpenAI)
- est: <$0.05
