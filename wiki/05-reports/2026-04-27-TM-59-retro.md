---
title: TM-59 retrospective — adversarial messaging
date: 2026-04-27
type: session
task_id: TM-59
tags: [retro, ai, security]
status: active
---

# TM-59 회고 — D1-D7 거절 메시지 명확화

## What went well

- 범위가 명확 (TM-45 보고서 D1-D7 분기 재라벨링) → 단일 모듈 + 2 wiring 지점으로 끝남.
- 카테고리 분류 + 메시지 + Error 클래스를 한 파일 (`src/lib/ai/refusal.ts`) 로 묶어 응집도 유지. 향후 카테고리 추가 시 영향 범위 작음.
- 유닛 테스트 41 + 통합 3 = 44 신규 케이스, 380 전체 회귀 0건.
- `unknown` 폴백 정책으로 false positive 가 legacy 동작을 침범하지 않도록 보수적 설계.

## What hurt

- 패턴 휴리스틱 한 줄 (`reveal/leak ... system|hidden ... prompt|instructions`) 이 첫 시도에 "reveal the hidden system prompt" 를 못 잡아 1회 수정. 사전에 매칭 케이스 머리 속 시뮬레이션 부족.
- 다국어 (영/한) 외엔 무지원 — 일본어/중국어 거절 시 `unknown` 폴백으로 떨어져 legacy 메시지 노출. 현 단계 기획 우선순위상 수용.

## What's next

- **TM-45 r3** 재검증: D1-D7 응답 status가 400 (`AI_REFUSAL_*`) 으로 바뀌는지 fuzz harness 로 확인. Orchestrator 가 자동 트리거.
- **클라이언트 toast 차별화** (선택) — `useStudio` 가 `code` 를 보고 카테고리별 아이콘/색을 적용할 수도 있음. 현재는 `error` 텍스트만 사용. follow-up task 후보.
- **모델별 거절 phrasing 수집** — gpt-4o-mini vs sonnet vs haiku 거절 문구 차이 모니터링 → 패턴 보강은 데이터 기반으로.

## 메트릭

- 코드 변경: +2 files (refusal.ts 174 LOC, refusal.test.ts 173 LOC), modified 3 (generate.ts +9, route.ts +12, generate.test.ts +37)
- 테스트: 380 passed (336 → 380, +44 신규), regressions 0
- 시간: ~25분 (분류기 설계/구현/테스트/와이어링/문서)
