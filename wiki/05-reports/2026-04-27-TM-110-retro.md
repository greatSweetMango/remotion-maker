---
date: 2026-04-27
type: retro
task: TM-110
tags: [type/retro, area/templates]
related: [TM-110, TM-84]
---

# TM-110 retro

## What worked
- Skepticism of the QA hypothesis (`Math.random()`) before patching. A
  five-second `grep` confirmed the call did not exist in the file, so the
  team-lead loop didn't burn a cycle implementing a no-op fix.
- A throw-away `/dev-particle-hydration` probe reproduced the warning in
  ~30s and was deleted before commit, keeping the diff minimal.

## What didn't
- The original QA r2 note lists `Math.random()` as the cause without
  having actually opened the file. Going forward, QA hypotheses about a
  named JS API should include a one-line citation (`grep` snippet) or be
  marked "speculative".
- corepack on this machine is broken (signature verification failure on
  pnpm fetch). Worked around with a `node_modules` symlink to the parent
  worktree's install — fine for one-off, but builds inside this worktree
  will fail until corepack is repaired or `pnpm install` is rerun
  manually. Tracked separately.

## Lock release
- TM-110 lock released. No follow-up tasks required for this template.
  An r3 audit of the other TM-38/39 templates for the same
  pixel-rounding hydration pattern is *optional* (they didn't fire the
  warning during TM-84 r2).
