---
title: "TM-43 retro (r2) — CSP revalidation"
created: 2026-04-27
updated: 2026-04-27
type: report
report_type: session
task_id: TM-43
qa_iteration: 2
tags: [retro, qa, csp, ref/TM-50]
status: completed
---

# TM-43 retro — r2 (CSP revalidation)

## 결과 verdict

**APPROVE** — 35/35 템플릿 응답에서 `media-src 'self' data: blob: https:` 정상 적용. r1 의 잔존 CSP 노이즈 해소. 신규 이슈 0.

## 잘된 점

- Deterministic verification path 선택 — 35개 시각 캡처를 재실행하지 않고, **루트 원인이 제거됐는지를 응답 헤더에서 직접 확인**. CSP 는 라우트 단위로 분기 없이 동일 단일 매처(`'/:path*'`)로 적용되므로 35회 헤더 probe 가 시각 검사보다 더 정확한 증거.
- TM-50 의 단위 테스트가 회귀 가드 역할을 충실히 수행 — 향후 누군가 `media-src` 를 좁히면 CI 에서 즉시 막힘.
- Worktree rebase 로 main 의 TM-50 변경을 자동 가져옴 — 작업 시작 시 `next.config.ts` 가 머지 직전 상태였던 점을 빠르게 감지.

## 문제 / 마찰

- **포트 충돌**: 지정된 3043 포트가 다른 worktree (별도 dev 인스턴스) 에 점유 중. 3143 으로 폴백. multi-worktree 병렬 운영 정책 (TM-43 r1 보고에 이미 명시) 의 실제 사례.
- **auth `MissingSecret` warning** — `.env.local` 의 `NEXTAUTH_SECRET` 미설정. 본 검증에서는 redirect (307) 자체가 헤더 검증에 충분하므로 무영향. 후속 시각 감사 시 인증 셋업 필요.
- **`scripts/setup-worktree.sh` 부재** — 지시에 명시됐으나 worktree 에 존재하지 않음. `npm ci` 만으로 충분.

## 다음 액션

- requalify trigger 해제 — `triggers_requalify: ["TM-43"]` 가 박힌 fix task 가 머지됐고 본 r2 에서 통과했으므로 TM-43 은 `done` 으로 전환 가능.
- 신규 fix task spawn 없음.
- 인증된 headless 브라우저 기반 시각 회귀 (Playwright + dev session) 는 별도 infra task 로 가치 — 본 task 에는 over-scope.

## 메트릭

- 진행 단계: rebase → unit test → dev boot → 35 라우트 probe → 산출물 35 + 보고서 1 + 회고 1
- 코드 변경: 0 (검증만; rebase 로 가져온 TM-50 변경은 이미 main).
- 비용: $0 (OpenAI 0회).
