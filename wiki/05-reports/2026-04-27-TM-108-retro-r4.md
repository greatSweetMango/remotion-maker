---
title: "2026-04-27 — TM-108 r4 회고: TM-114 머지 후 multi-step 가용성 회복 + 첫 클린 렌더 2/5"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, tm-108, ai/generate, qa]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (Claude Opus 4.7 1M)
qa_iteration: 4
---

# TM-108 r4 — 회고

## 무엇을 했나

- TM-114(multi-line require sanitize + broken destructure sweep, PR #115) 머지된 main 위에서 동일 5 케이스 재측정.
- baseline → 5/5 200 + judge 5/5 + avg **72.4** (r1 60.0 → r2 70.6 → r3 71.8 → r4 72.4 단조 상승).
- full(AI_MULTI_STEP=1) → **5/5 200 회복** + judge 5/5 + avg **49.4** + **클린 렌더 2/5** (case 4 chart, case 5 typo).
- r1→r2→r3→r4 추이 표 박제: [[2026-04-27-TM-108-quality-benchmark-r4|TM-108 r4 quality benchmark]].

## 잘 된 것

1. **TM-114 효과 검증**: r3 의 가용성 회귀(`SyntaxError 1:46`) 가 r4 에서 0 건 — sanitize 확장이 정확히 그 형태를 잡았다.
2. **첫 multi-step 클린 렌더**: r1~r3 동안 0/5 였던 클린 렌더가 r4 에서 2/5 진입 (case 4 chart=60, case 5 typo=88). multi-step 의 진짜 가치(복잡한 사양 prompt) 가 처음 측정에 잡힘.
3. **r4 manifest 보존**: 동시 실행 worktree 충돌 없이 baseline/full 양쪽 scores.json 정상 저장 — r3 의 측정 인프라 사고 재발 방지.
4. **추이 데이터 4 회차 누적**: 4 점 모두 갖춘 시계열 → ADR(multi-step default-off) 의 정량 근거 확보.

## 개선/배운 점

1. **잔존 EB 3/5 — render error 0 미달**: case 1 `<Scene1>`, case 2/3 `<Unknown>` 두 시그니처 패턴이 살아있다. multi-step 의 사용자 체감 안정성은 아직 60% 실패. 추가 evaluator/composer 패치 필요.
2. **multi-step 의 입력 의존성**: 짧은 prompt(case 1 spinner) 나 long-form(case 2 60s) 에서는 multi-step 이 오히려 손해 — multi-step 은 "복잡한 visual spec prompt 에서만 켜기" 라는 selective enablement 전략 검토 가치.
3. **bench infra 격리 미적용**: r3 후속 액션이 아직 미머지. r4 는 단독 실행으로 사고 회피했지만, 향후 병렬 측정 위해 결과 경로 격리 PR 필요 (Tier-1 recommendation).
4. **dev 재시작 패턴**: baseline → full 모드 전환 위해 dev 재시작 필요. AI_MULTI_STEP 가 런타임 토글 가능하면 측정 시간 1~2 분 단축 가능.

## 결론 / 다음 결정

- **multi-step default-off 정책 유지** — render error 3/5 + 평균 격차 23 점. production 승격 불가.
- **ADR 후보**: "multi-step 은 opt-in experimental flag" — 4 회차 시계열 데이터로 근거 확보. (별도 ADR PR 권고, 본 PR 은 측정/리포트만.)
- 새 fix task `AI-BUG-multi-step-evaluator-eb` spawn 권고 (잔존 EB 두 패턴). triggers_requalify=[TM-108]. 머지 시 r5 자동 재실행.
- bench infra 격리 PR 도 별도 분리 권고 (TM-108 r3 → r4 잔여).

## 산출물

- 본 PR (코드 변경 없음, wiki 보고서 + retro + 측정 결과 JSON/PNG):
  - `wiki/05-reports/2026-04-27-TM-108-quality-benchmark-r4.md`
  - `wiki/05-reports/2026-04-27-TM-108-retro-r4.md`
  - `__tests__/benchmarks/results/tm-108/baseline/{manifest,scores}.json` + screenshots/ (10 PNG)
  - `__tests__/benchmarks/results/tm-108/full/{manifest,scores}.json` + screenshots/ (10 PNG)

## 비용 / 시간

- OpenAI: gpt-4o judge 10 회 (baseline 5 + full 5) + multi-step generation 5 회. 추정 ~$1.4.
- 시간: setup → baseline (≈3min) → dev restart → full (≈4min) → judge → wiki write. 총 ≈18min (timeout 20min 안에 완주).

## 다음 회차 트리거

- `AI-BUG-multi-step-evaluator-eb` fix 머지 → r5 자동 재실행 (qa_iteration=5).
- 또는 multi-step opt-in ADR 머지 후 별도 측정 불필요 시 TM-108 closed 처리.
