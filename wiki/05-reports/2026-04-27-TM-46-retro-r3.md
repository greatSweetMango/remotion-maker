---
title: TM-46 r3 회고 — 풀 30 prompts 실행
created: 2026-04-27
updated: 2026-04-27
tags: [qa, retro, r3]
status: active
report_type: session
period: 2026-04-27
author: TeamLead (claude opus 4.7 1M)
---

# TM-46 r3 회고

## 잘 된 점

1. **r2 인프라 그대로 동작** — `tm-46-run-r2.ts` driver + judge.ts (TM-66 OpenAI gpt-4o) 가
   추가 수정 없이 30 풀런 처리. 인프라 박제가 r2 에서 제대로 끝났음 확인.
2. **자동화 루프 완성** — driver(28/30) → judge(28 OpenAI 호출) → followup task spawn(10건)
   까지 사람 개입 0회.
3. **카테고리 차별화 시각화** — loader 85 vs data-viz 44.5 의 큰 분산이 다음 개선 우선순위를
   명확히 함. 단일 평균 71.2 보다 카테고리 평균이 의사결정에 유효.
4. **judge calibration 신뢰도** — 같은 카테고리 안에 80+ 와 15-23 이 공존 (data-viz dv-06=80
   vs dv-02=15). 루브릭이 케이스 차이를 잘 잡음.

## 아쉬운 점

1. **acceptance 미달 (71.2 < 75)** — 단순 prompt 보강만으로는 부족할 수 있음. data-viz
   templates 자체가 부재할 가능성. r4 에서는 templates 추가 + system-prompt 강화 병행 필요.
2. **generate 500 2건 (dv-01/dv-10)** — TM-69 가 머지되어야 해소. 본 회차 표본 28/30 으로
   data-viz 평균이 4 케이스만으로 산출됨 (분산 큼).
3. **judge cost 추정 오차** — spec 의 "$3" 대비 실측 ~$0.6. 추정 모델이 비싼 멀티모달 가정.
   다음부터는 첫 회차 측정값을 spec 에 반영.
4. **dev 서버 sandbox 종료 신호 노이즈** — `npm run dev &` 가 즉시 exit 0 으로 보고됨
   (자식 node 는 살아 있음). 향후 setup-worktree.sh 가 readiness probe 까지 포함하면 좋음.

## Action Items

- [ ] **TM-69 머지 우선** — generate JSON 파싱 안정화. 머지 후 dv-01/dv-10 재시도.
- [ ] **r4 재실행** — 위 10 task 중 prompt-only 8건 처리 + TM-69 머지 후. acceptance 75 재시도.
- [ ] **templates 보강 follow-up task** — data-viz 카테고리 (특히 단순 막대그래프, pie chart) 의
      reference 템플릿이 너무 적은지 grep 검토. 부족하면 별도 task spawn.
- [ ] **transition motion 시스템 검토** — A→B 상태 전환 자체가 약함. system-prompt 에 "must show
      progression across 3 frames" 조항 강화 검토.

## 비용 / 시간

- 사람 시간 (TeamLead session): 본 회차 약 60분 (대기 dominant).
- API: ~$0.6 OpenAI.
- task-master: 10 add-task call.
