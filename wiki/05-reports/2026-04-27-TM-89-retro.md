---
title: "2026-04-27 — TM-89 retro"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead
---

# TM-89 retro

## TL;DR

TM-75 가 사전에 발견·문서화해 둔 덕분에 fix 자체는 단순 (entry 분리 + alias 주입). 추가 surprise 1건: 단순 entry-swap 으로는 부족하고 `@/*` alias 도 필요했다 — VR 드라이버의 `webpackOverride` 가 이미 동일 처리를 하고 있었기 때문에 5분 내 식별.

## 잘된 점

- TM-75 의 `vr-entry.tsx` 가 이미 정확한 reference pattern 이었다 → 디자인 결정 시간 0.
- 단위 테스트를 standalone bundler 로 직접 돌려 fix 가 실제로 동작함을 확인 (lab green).
- `bundleCache` 모듈 변수가 그대로 살아있어 첫 호출만 느리고 이후는 무비용.

## 아쉬운 점 / 학습

- `tsconfig` 의 `@/*` path 가 standalone webpack 에는 자동 주입되지 않는다는 점은 TM-75 회고에도 명시되어 있었지만 위키 status/architecture 에 반영되지 않아 한 번 더 trip 했다 → 본 보고서로 명시.
- `selectComposition` 이 root 등록 여부에 대한 에러 메시지가 다소 모호 → 후속자가 동일 함정에 빠지지 않도록 export-entry.tsx 코멘트에 rationale 보존.

## 후속

- [ ] requalify TM-75 (entry 가 변하지 않았으므로 baseline 영향 0 예상)
- [ ] export route 의 e2e 통합 테스트는 Chrome Headless Shell 다운로드 (~90MB) 가 필요해 기본 jest run 에서는 skip → CI lane 분리 검토
