---
title: "2026-04-27 — TM-62 회고"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retrospective]
status: active
report_type: session
period: "2026-04-27"
author: TeamLead (TM-62)
task_id: TM-62
---

# TM-62 회고

## 결과

- ParticlePhysics 시각 결함 수정 완료. start/mid/end 3컷 캡처로 검증.
- 변경 한 파일, 8개 라인 수정 + 4개 라인 추가. 표면적 작음.

## 잘된 점

- **원인 분석을 먼저 했다.** "중력 약함" 외에 진짜 culprit는 `vx *= 0.92` 마찰이라는 걸 코드 정독에서 잡아냄. 마찰만 손봐도 절반 이상 해결.
- TM-43 audit 보고서에 의존해 reproduce 단계 생략. 보고서가 살아있어 디버깅 cycle 1회로 끝.
- worktree 격리 — 다른 세션 영향 없이 dev:3062에서 검증.

## 아쉬운 점 / 학습

- `127.0.0.1:3062` 접근 시 다른 worktree들의 `127.0.0.1:30NN` cookie가 cross-port 공유되어 자동 redirect. **`localhost:3062` 사용으로 회피**. TM-43에서 동일 이슈 보고됐으나 host 분리 정책이 아직 강제되지 않음 → orchestrator 수준에서 worktree마다 unique host (`127.0.0.x`) 부여 또는 cookie `__Host-` prefix 적용을 ADR로 굳힐 필요.
- dev 서버가 캡처 도중 한 번 죽음 (재시작 1회). 원인 미상 — ts compile error는 아니었음. 향후 setup-worktree에 health-poll + auto-restart 옵션 검토.
- 물리 파라미터 튜닝은 시각적 판단이라 회귀 테스트 어려움. screenshot baseline diff (perceptual hash) 도입 후보 (별도 task).

## triggers_requalify

`[43, 46]` — TM-43은 본 fix를 반영해 row 갱신, TM-46이 같은 템플릿 의존 시 재검수.

## 후속 후보 task

- (env) CSP `media-src` data: 허용 — 35 템플릿 공통 5건 콘솔 ERROR 제거.
- (infra) worktree per-host cookie 격리 ADR.
- (qa) screenshot baseline regression — `wiki/05-reports/screenshots/baseline/`.
