---
title: "TM-109 retro — template quota guard"
created: 2026-04-27
updated: 2026-04-27
tags: [retro, fix, api/edit, quota]
status: active
report_type: session
task_id: TM-109
author: TeamLead
---

# TM-109 회고

## 결과

- TM-106 retro follow-up #1 처리 완료.
- `Asset.templateSourceId` 컬럼 + `/api/edit` 가드 + 단위 테스트 2건.
- OpenAI 호출 0 (모킹된 단위 테스트로 충분).

## 무엇이 잘 되었나

- TM-106 retro에 follow-up이 명확히 적혀 있어 RCA 단계 거의 0. 코드 한 곳(`route.ts`) 만 보면 됐음.
- TM-93 `reserveEditSlot` 과 `effectiveAssetId` pivot 의 상호작용이 잠시 헷갈렸으나, "template- 재전송 시 reservation도 동일 키 → 그 키 자체가 effectiveAssetId 이므로 그대로 둬도 무방" 으로 정리됨. 향후 클라이언트가 정상 pivot하면 reservation 키도 자동으로 cuid로 이동함.
- 단위 테스트가 5회 sequential edit + 1회 first-materialize 두 case로 분리되어 가드 진입/미진입을 모두 커버.

## 무엇이 아쉬웠나

- 라이브 e2e (Playwright)는 본 task 범위 외로 미룸 — TM-106 retro의 follow-up #2는 그대로 남음.
- 만일 user가 *서로 다른* template을 5개 적용하면 `monthlyUsage = 5` 가 정상이지만, 본 테스트는 그 케이스를 명시적으로 검증하지 않음 (가드 로직상 `templateSourceId` 다르므로 자연 동작).
- DB 마이그레이션 자동화 X — `prisma db push` 수동.

## 시간/비용

- OpenAI 호출: 0
- 작업 시간: ~20분 (RCA + 스키마 + 라우트 + 테스트 + wiki)
- 예산 (medium) 대비 충분히 내.

## 후속

- TM-106 retro follow-up #2 (Playwright e2e) 그대로 잔존.
- 본 가드는 *재머터리얼라이즈*만 막는다. *동일 template-id를 다른 user가 사용*하는 경우는 각자 1슬롯씩 정상 차감 (의도된 동작).
