---
title: TM-46 r7 retro — RAG ablation 첫 측정
created: 2026-04-27
updated: 2026-04-27
tags: [qa, retrospective, r7, rag, ablation]
status: active
report_type: session
period: 2026-04-27
author: TeamLead (claude opus 4.7 1M)
task_id: TM-46
qa_iteration: 7
---

# TM-46 r7 — Retrospective (RAG ablation, partial)

## 한 줄 요약

paired n=8 비교에서 **RAG 의 시각 품질 영향 = +0.25 (round 0)**. 평균은 영향 없으나
prompt-level 분산 ±20pt 큼 — RAG 가 좋은 케이스/나쁜 케이스 양쪽으로 작용 (wash).
RAG 자체보다 reference 매칭 품질이 핵심 가설.

자세한 결과: [[2026-04-27-TM-46-visual-judge-r7-rag-impact]].

## 무엇을 했나

1. `RAG_DISABLE=1` env 토글 추가 (`src/lib/ai/generate.ts`).
2. `__tests__/benchmarks/tm-46-r7-capture.ts` — 모드별 캡처 driver + soft deadline + ID filter.
3. `__tests__/benchmarks/tm-46-r7-analyze.ts` — paired comparison + ADR-0016 4기준.
4. `__tests__/benchmarks/tm-46-judge.ts` — `--out` flag 추가 (모드별 score 분리).
5. RAG-ON capture 8/30 + RAG-OFF capture 14/30 reliable + judge 양쪽.

## 무엇이 잘 안 됐나

1. **dev server kill 타이밍**: 첫 RAG-ON capture (5:08) 가 9번째 prompt 처리 중일 때 dev 를
   kill 했음 (5:10:51) — RAG-OFF 모드로 전환하기 위해. capture 프로세스는 살아있어 ECONNREFUSED.
   첫 8 prompt 만 보존됨.
2. **2번째 dev kill 도 같은 실수**: capture-off (n=30) 진행 중일 때 (5:15:33) capture-on2 를
   위해 dev 를 RAG-ON 으로 재기동. capture-off 의 14번째 prompt 이후 generation 은 RAG-ON 으로
   contaminated. 분석 시 14 IDs 까지만 reliable 로 필터링.
3. **monitor false "DONE"** 이벤트: tail -F + grep 으로 진행 상황 추적 시 새 dev 로그 라인이
   capture 라인과 섞이며 false 매칭 발생. 신뢰성 떨어짐 — 직접 manifest 확인 더 안전.

## 회차 진행 표

| 회차 | n_on | n_off | 비고 |
|---:|---:|---:|---|
| r1-r6 | (단일 변수, 결정성 누적) | — | r6 round std=1.77 도달 |
| **r7** | **8 partial** | **14 reliable** | **RAG ablation 첫 측정** |

## 비용

- OpenAI gpt-4o judge: ~22 successful judgments × $0.05 = ~$1.1.
- gpt-4o-mini capture (~30 generations RAG-OFF + ~30 RAG-ON 시도): ~$0.6.
- 합계 ~$1.7 (예산 $3 이하).

## 다음 회차 (r8) 가설/실행안

1. **단일 RAG 모드 / 단일 dev** (RAG_DISABLE 변경 시 dev 재기동 필수, capture 정지 보장)
2. RAG-ON n=30 + RAG-OFF n=30 fully paired (~14-16 분 capture × 2 = 30 분 → budget 35 min 권장)
3. paired Δ 신뢰도 평가 — n=8 → n=30 으로 확대 시 paired t-test 적용 가능.
4. RAG 가 평균은 wash 라는 가설이 확인되면 → **multi-reference RAG (TM-?)** 로 확장 권장.

## 자동화 정책 위반 / escalate 사항

- 없음. budget 25 분 안에 partial 결과로 마감 (지시 준수).
- 새 의존성 추가 없음, DB migration 없음, prod 배포 없음.

## 락 상태 (Orchestrator 가 처리)

- branch-locks.json: TM-46 락 해제 권장 (회차 종료, fix task 미생성 → 재실행 trigger 없음).

## 참고

- ADR-0019 (RAG templates) — baseline 가설 출처
- ADR-0016 (acceptance gate v2) — 4 기준 평가
- 직전 회차: `2026-04-27-TM-46-visual-judge-r6.md`, `2026-04-27-TM-46-retro-r6.md`
