---
title: TM-87 회고 — worktree prisma client 격리
created: 2026-04-27
updated: 2026-04-27
tags: [retro, infra, worktree, prisma]
status: active
report_type: session
period: 2026-04-27
author: TeamLead (claude opus 4.7 1M)
task_id: TM-87
---

# TM-87 회고

## 잘 된 점

1. **TM-46 r6 회고의 follow-up 을 즉시 task 로 spawn → 머지** — "워크트리 prisma
   client isolate 부재" 라는 회고 문장이 바로 다음 iteration 에서 픽스로 이어짐.
   회고 → infra fix 의 사이클이 1 hop.
2. **근본 원인 정확히 진단** — 단순히 `prisma generate` 한 줄 추가하는게 아니라,
   "worktree 에 `@prisma/client` 패키지가 없으면 prisma CLI 자체가 main 의
   client 디렉토리에 generate 한다" 는 두 번째 함정을 미리 발견. 검증 단계에서
   `cp -R @prisma .prisma` seeding 이 필요함을 확인.
3. **멱등성 검증을 spec 단계에서 명시 → 2회 실제 실행으로 확인**. 회귀 위험 0.

## 아쉬운 점

1. **node_modules 디스크 사용량 약 145M/워크트리** — 깨끗한 해결책은 아니다.
   장기적으로 `pnpm` 도입 또는 worktree node_modules 를 main 으로 symlink 하는
   별도 infra 작업이 더 나을 수 있음.
2. **본 fix 가 적용되기 전 생성된 worktree 에는 자동 적용되지 않음** — Orchestrator
   가 기존 worktree 들에 대해 `setup-worktree.sh` 재실행을 강제해야 안전. 별도
   안내 필요.
3. **TM-46 r6 의 임시 한도 상향 (`src/lib/usage.ts` PRO=100000) 되돌리기 미포함**
   — 본 task 의 정의에 없었으므로 별도 follow-up 으로 분리. 누락 위험.

## 다음에 시도

1. **worktree node_modules pnpm/symlink 전환 평가** — 145M × N worktree 누적 시
   디스크 압박. content-addressable store 로 전환하면 사실상 0 추가.
2. **기존 active worktree 일괄 re-bootstrap 스크립트** — Orchestrator iter 시작
   시 모든 active worktree 에 대해 `setup-worktree.sh` 멱등 재실행하는 hook.
3. **`src/lib/usage.ts` PRO 한도 200 복구** — TM-87 머지 후 별도 PR (follow-up
   task: AI-INFRA-revert-temp-usage-limit).

## 변경 요약

| 항목 | 값 |
|---|---|
| 파일 변경 | `scripts/setup-worktree.sh` (+~25줄), 신규 wiki note + retro |
| 새 의존성 | 없음 |
| db migration | 없음 |
| 멱등성 | 검증 완료 (2회 실행 동일) |
| 디스크 영향 | worktree 당 +~145M (`node_modules/@prisma` + `.prisma`) |

## 관련 문서

- 기술 노트: [[02-dev/tech-notes/2026-04-27-TM-87-fix|TM-87 fix note]]
- 발견 회고: [[05-reports/2026-04-27-TM-46-retro-r6|TM-46 r6 retro]] §아쉬운 점 3
- 영향 받는 파일: `scripts/setup-worktree.sh`
