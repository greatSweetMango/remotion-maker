---
title: 'TM-19 QA Report — spend.json autotrack hook'
created: 2026-04-26
updated: 2026-04-26
tags: [report, qa, infra, agent-company]
status: active
report_type: session
---

# TM-19 QA Report

## 범위

- `.claude/hooks/post-tool-use.sh` (신규)
- `.claude/hooks/session-start.sh` (수정)
- `.claude/settings.json` (PostToolUse 등록)

## 정적 검증

- `bash -n` 두 스크립트 syntax 통과
- `python3 -m json.tool` settings.json + spend.json 통과
- chmod +x 적용 확인

## 단위 시뮬레이션

- 가격 계산 (python 재현): Opus 4.5, 1000 in / 500 out / 2000 cache_read / 0 cache_creation → 0.055500 USD ✅ (예상치 일치)
- jq 미설치 환경: post-tool-use.sh가 spend.json 변경 없이 exit 0 ✅
- session-start.sh: STOP 파일 없음 + spend.json `date: null` 상태에서 exit 0 ✅

## 미실행 (실환경 의존)

- 실제 Claude API 호출 후 PostToolUse hook 실행 → spend.json `current.cost_usd` 증가 확인
- 자정 리셋: `current.date`를 어제로 강제 → SessionStart 후 history archive
- 95% 도달: 합성 spend.json 으로 `continue: false` 출력 검증

위 3건은 jq 설치 + Claude Code 라이브 세션이 필요하므로 머지 후 첫 세션에서 자연 검증.

## 회귀

- 기존 SessionStart 훅의 STOP 파일 / daily 95% 분기 보존 ✅
- 기존 PreToolUse(Bash) / Stop 훅 변경 없음 ✅

## 결론

APPROVE — 정적 검증 통과. 라이브 세션 검증은 머지 직후 자연 발생.
