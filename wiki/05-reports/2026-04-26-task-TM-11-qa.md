---
title: "QA Report — TM-11 GEN-06 Clarifying Questions"
created: 2026-04-26
updated: 2026-04-26
tags: [report, qa]
task_id: TM-11
status: active
---

# QA Report — TM-11 (GEN-06 AI 역질문)

> **검증 디렉토리**: `worktrees/TM-11-gen-06-clarifying-questions/`
> **대상 commit**: `4274c6e`

## 정량 결과

| 항목 | 값 |
|---|---|
| 회귀 테스트 | **28/28 pass** (5 suites, 19 기존 + 9 신규) |
| TypeScript | **0 errors** (`npx tsc --noEmit`) |
| Lint (신규 에러) | **0건** (PromptPanel:129 setMode useEffect는 main 기존 코드) |
| diff stat | 9 파일, +510/-29 |

## 시나리오 검증 (mental walk-through, 코드 추적)

| # | 시나리오 | 결과 | 흐름 |
|---|---|---|---|
| 1 | 모호 ('애니메이션 만들어줘') | ✅ | useStudio.generate → POST /api/generate → generate.ts(no [USER ANSWERS]) → LLM mode='clarify' → route.ts return type:'clarify' (쿼터 미차감) → SET_CLARIFY → ClarifyCard 렌더 |
| 2 | 명확 ('빨간 카운터 0~100, 3초') | ✅ | LLM이 system prompt 휴리스틱(<10 words + 키워드) 따라 mode='generate' → SET_ASSET |
| 3 | 답변 후 재생성 | ✅ | ClarifyCard.onSubmit → submitClarifyAnswers → generate(pendingPrompt, answers) → buildUserMessage가 [USER ANSWERS] 블록 prepend → system prompt 규칙 "[USER ANSWERS] 있으면 ALWAYS generate" 발동 |
| 4 | skip | ✅ | ClarifyCard.onSkip → skipClarify → generate(pending, {__skip__:'true'}) → forced generate |

## 비용 추정 (Anthropic Haiku 3.5: in $0.80/MTok, out $4/MTok)

| 케이스 | input tok | output tok | 비용 |
|---|---|---|---|
| **mode=clarify** (모호) | ~1,450 | ~150 | **~$0.00176** |
| **mode=generate** (명확/답변 후) | ~1,500 | ~1,200 | **~$0.00600** |
| 모호→clarify→generate worst case | (clarify 1.76) + (generate 6.00) | | **~$0.0078** |

**system prompt**: 5,769 chars (~1,440 tokens). RESPONSE MODE DECISION 블록은 +1,977 chars (~+495 tokens vs 기존). prompt caching으로 generate 비용 증가분 완화 가능.

## 아키텍처 발견

- **단일 LLM 호출 토글** (clarify와 generate를 하나의 system prompt에서 분기). 컨텍스트 가정("clarify 1회 추가 호출")과 달리, 명확한 프롬프트는 호출 수가 늘지 않음 — 더 효율적.
- clarify 응답 시 monthlyUsage 미차감 (`route.ts:47`) — 무료 한도 보호 ✅
- 빈 answers는 `Object.keys(...).length === 0` noop 처리 → 하위호환 ✅

## 발견된 이슈

없음. PromptPanel:129 사전 lint 1건은 main 기존 코드 (블레임 확인).

## 권장사항

- 라이브 Playwright 시나리오는 ANTHROPIC_API_KEY가 필요한 라이브 호출 → 별도 task로 분리 권장 (`#qa` `#integration-test`)
- Skip 경로의 generate 호출 캐싱 검토 (선택)

## 관련

- [[../01-pm/decisions/0005-clarify-flow-architecture|ADR-0005]]
- [[2026-04-26-task-TM-11-validation|VL-1 Validation]]
- 코드: branch `TM-11-gen-06-clarifying-questions` commit `4274c6e`
