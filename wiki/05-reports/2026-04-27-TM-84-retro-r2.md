---
title: "2026-04-27 — TM-84 r2 회고 (rate-limit retry, 통과)"
created: 2026-04-27
updated: 2026-04-27
tags: [report, retro]
status: active
report_type: incident
period: "2026-04-27"
author: "TeamLead-TM-84-r2"
provenance: extracted
---

# TM-84 r2 회고

## 결과

- r1: P1×2, P2×1 발견 → TM-80 (= TM-98) spawn → fix 머지 → r2 재검증 통과
- r2: 5/5 templates × 3/3 viewport 모두 layout 정상. 잔여 이슈는 layout 외 (ParticlePhysics hydration).

## r2 회차 자체 회고

### r1 → r2 사이 차질
- r1 회차가 **rate limit hit** 으로 partial 종료됨 (이전 transcript 의 안내)
- r2 시작 시 발견:
  - 포트 3084 에 **5시간+ 묵은 next-server orphan** 점유 → setup-worktree.sh 가 EADDRINUSE 로 dev 시작 실패. r1 종료 시 dev 프로세스 cleanup 누락.
  - `setup-worktree.sh` usage 가 `<worktree_path> <dev_port>` 인데 지시는 `setup-worktree.sh 3084 127.0.0.1` 로 와서 첫 인자 검증(numeric)에 걸림 → 정정 후 정상 진행.

### 잘된 점
- TM-98 fix 의 측정 가능한 변경 (`hidden lg:hidden`, `lg:flex`, `flex-wrap`, `min-w-0`) 가 r1 의 측정값을 직접 뒤집음 — 회귀 검증이 정량으로 가능했음.
- Playwright `browser_evaluate` 로 docScrollW / `__remotion-player` getBoundingClientRect 측정 → "보인다/안 보인다" 가 아닌 숫자로 통과 판정.

### 개선 제안
- **dev orphan cleanup**: 향후 worktree 종료 hook 에서 `lsof -ti:<PORT> | xargs kill` 자동화 검토. (Phase F JSON 으로 PM 에 제안)
- **setup-worktree.sh 인자 안내 통일**: orchestrate.md / team-lead.md 의 예시 명령에 `bash scripts/setup-worktree.sh "$(pwd)" 3084 127.0.0.1` 처럼 worktree path 를 명시.
- 30 template 전수 검증은 manual 5/iter 로는 비효율 — Playwright 스크립트 1회 작성 후 batch 로 (별도 task).

### 락 / requalify 상태
- TM-84 통과 조건 충족. requalify 락 해제.
- 새 task 권장: **AI-NEW-particle-physics-hydration-fix** (P3, optional, layout 무관)
