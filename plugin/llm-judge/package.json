{
  "name": "@remotion-maker/mcp-llm-judge",
  "version": "0.1.0",
  "private": true,
  "description": "LLM-as-judge MCP server — exposes judge_visual / judge_code over stdio (OpenAI multimodal, deterministic).",
  "type": "module",
  "bin": {
    "mcp-llm-judge": "./dist/server.js"
  },
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --loader ts-node/esm src/server.ts",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "openai": "^4.67.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
